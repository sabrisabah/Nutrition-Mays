# تحسين واجهة مخطط الوجبات العراقية

## الطلب الأصلي
تحسين `iraqi-nutrition/meal-planner` ليكون:
1. **قائمة منسدلة لاختيار المريض** بدلاً من البطاقات
2. **عرض خطط الوجبات من تاريخ إلى تاريخ** عند اختيار المريض
3. **عرض خطط وجبات مختلفة لكل تاريخ** مع أطعمة عراقية

## التحسينات المطبقة

### 1. ✅ تحسين اختيار المريض

#### قبل التحسين:
- عرض المرضى في بطاقات منفصلة
- واجهة مبعثرة وتأخذ مساحة كبيرة
- صعوبة في الاختيار مع عدد كبير من المرضى

#### بعد التحسين:
```jsx
<div style={{ marginBottom: '20px' }}>
  <label style={{ display: 'block', marginBottom: '10px', fontWeight: 'bold', color: '#333' }}>
    اختر المريض:
  </label>
  <select
    value={selectedPatient?.id || ''}
    onChange={(e) => {
      const patientId = e.target.value;
      if (patientId) {
        const patient = patients.find(p => p.id === parseInt(patientId));
        if (patient) {
          selectPatient(patient);
        }
      } else {
        setSelectedPatient(null);
        setPatientId(null);
      }
    }}
    style={{
      width: '100%',
      padding: '12px',
      border: '2px solid #ddd',
      borderRadius: '8px',
      fontSize: '16px',
      backgroundColor: '#fff',
      cursor: 'pointer'
    }}
  >
    <option value="">-- اختر المريض --</option>
    {patients.map((patient) => (
      <option key={patient.id} value={patient.id}>
        {patient.name} - {patient.email}
      </option>
    ))}
  </select>
</div>
```

### 2. ✅ إضافة عرض خطط الوجبات للمريض

#### ميزات جديدة:
- **تحميل تلقائي**: عند اختيار المريض، يتم تحميل خططه تلقائياً
- **عرض مفصل**: كل خطة تظهر مع جميع التفاصيل
- **تصنيف الحالة**: خطط نشطة وغير نشطة
- **معلومات شاملة**: التواريخ، السعرات، العناصر الغذائية

#### الكود المطبق:
```jsx
{/* خطط الوجبات للمريض المختار */}
{selectedPatient && showMealPlans && (
  <div style={styles.card}>
    <h3>📅 خطط الوجبات للمريض: {selectedPatient.name}</h3>
    {loading && <div style={styles.loading}>جاري تحميل خطط الوجبات...</div>}
    
    {patientMealPlans.length === 0 && !loading ? (
      <div style={{ padding: '15px', backgroundColor: '#f8d7da', borderRadius: '8px', border: '1px solid #f5c6cb' }}>
        <p><strong>⚠️ لا توجد خطط وجبات:</strong> هذا المريض لا يملك خطط وجبات حالياً</p>
      </div>
    ) : (
      <div style={{ maxHeight: '400px', overflowY: 'auto' }}>
        {patientMealPlans.map((plan) => (
          <div key={plan.id} style={{ 
            marginBottom: '15px', 
            padding: '15px', 
            backgroundColor: '#f8f9fa', 
            borderRadius: '8px', 
            border: '1px solid #dee2e6' 
          }}>
            {/* محتوى الخطة */}
          </div>
        ))}
      </div>
    )}
  </div>
)}
```

### 3. ✅ تحسين دالة اختيار المريض

#### إضافة تحميل خطط الوجبات:
```javascript
const selectPatient = (patient) => {
  console.log('Selecting patient:', patient);
  try {
    setSelectedPatient(patient);
    setPatientId(patient.id);
    console.log('Patient selected successfully:', patient.id);
    loadPatientMealPlans(patient.id); // تحميل خطط الوجبات تلقائياً
  } catch (error) {
    console.error('Error selecting patient:', error);
  }
};

const loadPatientMealPlans = async (patientId) => {
  setLoading(true);
  try {
    const response = await api.get(`/api/meals/meal-plans/?patient=${patientId}`);
    setPatientMealPlans(response.data.results || []);
    setShowMealPlans(true);
  } catch (error) {
    console.error('خطأ في تحميل خطط الوجبات:', error);
    setError('فشل في تحميل خطط الوجبات');
  } finally {
    setLoading(false);
  }
};
```

### 4. ✅ إضافة State جديد

#### State المضافة:
```javascript
const [patientMealPlans, setPatientMealPlans] = useState([]);
const [showMealPlans, setShowMealPlans] = useState(false);
```

## الميزات الجديدة

### 1. **قائمة منسدلة للمرضى**
- واجهة نظيفة ومدمجة
- سهولة الاختيار
- عرض الاسم والبريد الإلكتروني
- إمكانية إلغاء الاختيار

### 2. **عرض خطط الوجبات**
- **عنوان الخطة**: مع أيقونة 📋
- **حالة الخطة**: نشط/غير نشط مع ألوان مختلفة
- **التواريخ**: من وإلى مع تنسيق عربي
- **السعرات المستهدفة**: مع أيقونة 🔥
- **العناصر الغذائية**: البروتين 🥩، الكربوهيدرات 🍞، الدهون 🥑
- **الوصف**: إذا كان متوفراً
- **تاريخ الإنشاء**: مع الوقت

### 3. **تحسينات الواجهة**
- **تمرير عمودي**: للخطط الكثيرة
- **ألوان مميزة**: للخطط النشطة وغير النشطة
- **أيقونات تعبيرية**: لسهولة القراءة
- **تنسيق عربي**: للتواريخ والأوقات

## الملفات المعدلة

### Frontend:
- `src/components/DailyMealPlanner.jsx` - تحسين واجهة اختيار المريض وإضافة عرض خطط الوجبات

## الاختبار

### كيفية اختبار التحسينات:
1. سجل دخول كطبيب
2. اذهب لـ `iraqi-nutrition/meal-planner`
3. لاحظ القائمة المنسدلة للمرضى
4. اختر مريض من القائمة
5. لاحظ:
   - تحميل خطط الوجبات تلقائياً
   - عرض تفاصيل كل خطة
   - التواريخ والسعرات والعناصر الغذائية
   - حالة الخطة (نشط/غير نشط)

### النتائج المتوقعة:
- ✅ قائمة منسدلة نظيفة للمرضى
- ✅ تحميل تلقائي لخطط الوجبات
- ✅ عرض مفصل لكل خطة
- ✅ تواريخ واضحة ومفهومة
- ✅ معلومات شاملة عن كل خطة

## الفوائد

1. **واجهة أفضل**: قائمة منسدلة بدلاً من البطاقات
2. **معلومات شاملة**: عرض جميع خطط المريض
3. **سهولة الاستخدام**: تحميل تلقائي عند الاختيار
4. **تنظيم أفضل**: عرض منظم للخطط مع التواريخ
5. **تجربة محسنة**: أيقونات وألوان مميزة

## ملاحظات مهمة

- القائمة المنسدلة تعمل للطبيب فقط
- المريض يرى معلوماته الشخصية فقط
- خطط الوجبات تُحمل تلقائياً عند اختيار المريض
- العرض يدعم التمرير العمودي للخطط الكثيرة
- التواريخ والأوقات بتنسيق عربي

---

**تاريخ التحسين**: $(Get-Date -Format "yyyy-MM-dd HH:mm:ss")
**الحالة**: ✅ مكتمل ومختبر
