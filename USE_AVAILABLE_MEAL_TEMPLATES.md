# استخدام قوالب الوجبات المتاحة للتنويع اليومي

## التحديث المطبق
تحديث ميزة مخطط الوجبات العراقية للفترة الزمنية لاستخدام قوالب الوجبات المتاحة في النظام بدلاً من مجرد تغيير الأسماء.

## المشكلة السابقة
كانت الوجبات اليومية تظهر نفس المكونات مع تغيير في الأسماء فقط، مما لا يوفر تنويعاً حقيقياً في الوجبات.

## الحل المطبق

### 1. **🔄 استخدام قوالب الوجبات المتاحة:**

#### الكود المطبق:
```javascript
// دالة لتوليد الوجبات اليومية المختلفة من قوالب الوجبات المتاحة
const generateDailyMealVariations = (dietType, days) => {
  const variations = [];
  
  // تصفية قوالب الوجبات المتاحة حسب نوع النظام الغذائي
  const availableTemplates = mealTemplates.filter(template => {
    const templateName = (template.name_ar || template.name).toLowerCase();
    const dietTypeLower = dietType.toLowerCase();
    
    // مطابقة نوع النظام الغذائي
    if (dietTypeLower.includes('كيتو') || dietTypeLower.includes('keto')) {
      return templateName.includes('كيتو') || templateName.includes('keto');
    } else if (dietTypeLower.includes('متوسطي') || dietTypeLower.includes('mediterranean')) {
      return templateName.includes('متوسطي') || templateName.includes('mediterranean');
    } else if (dietTypeLower.includes('بروتينية') || dietTypeLower.includes('protein')) {
      return templateName.includes('بروتينية') || templateName.includes('protein');
    } else if (dietTypeLower.includes('فقدان') || dietTypeLower.includes('weight_loss')) {
      return templateName.includes('فقدان') || templateName.includes('weight_loss');
    } else if (dietTypeLower.includes('زيادة') || dietTypeLower.includes('weight_gain')) {
      return templateName.includes('زيادة') || templateName.includes('weight_gain');
    } else if (dietTypeLower.includes('منخفض') || dietTypeLower.includes('low_carb')) {
      return templateName.includes('منخفض') || templateName.includes('low_carb');
    } else if (dietTypeLower.includes('سكري') || dietTypeLower.includes('diabetic')) {
      return templateName.includes('سكري') || templateName.includes('diabetic');
    } else {
      // النظام المتوازن - استخدام جميع القوالب المتاحة
      return true;
    }
  });

  // إذا لم توجد قوالب مطابقة، استخدم القوالب المتاحة
  const templatesToUse = availableTemplates.length > 0 ? availableTemplates : mealTemplates;

  for (let day = 1; day <= days; day++) {
    // اختيار قالب مختلف لكل يوم
    const templateIndex = (day - 1) % templatesToUse.length;
    const selectedTemplate = templatesToUse[templateIndex];
    
    const dayVariation = {
      day: day,
      date: new Date(new Date(startDate).getTime() + (day - 1) * 24 * 60 * 60 * 1000),
      template: selectedTemplate,
      meals: selectedTemplate.meals ? selectedTemplate.meals.map((meal, index) => ({
        ...meal,
        name: `${meal.name} - اليوم ${day}`,
        day: day,
        template_name: selectedTemplate.name_ar || selectedTemplate.name
      })) : []
    };
    variations.push(dayVariation);
  }
  
  return variations;
};
```

### 2. **📊 عرض اسم القالب المستخدم:**

#### الكود المطبق:
```javascript
{dayPlan.template && (
  <div style={{ 
    marginBottom: '15px', 
    padding: '8px', 
    backgroundColor: '#d4edda', 
    borderRadius: '6px', 
    border: '1px solid #4caf50',
    textAlign: 'center'
  }}>
    <strong style={{ color: '#2e7d32' }}>
      🍽️ قالب الوجبات: {dayPlan.template.name_ar || dayPlan.template.name}
    </strong>
    {dayPlan.template.description && (
      <div style={{ fontSize: '0.9em', color: '#2e7d32', marginTop: '5px' }}>
        {dayPlan.template.description}
      </div>
    )}
  </div>
)}
```

### 3. **🥘 عرض المكونات الفعلية:**

#### الكود المطبق:
```javascript
<div style={{ marginBottom: '8px' }}>
  <strong style={{ fontSize: '0.8em' }}>المكونات:</strong>
  <ul style={{ margin: '5px 0', paddingRight: '15px', fontSize: '0.8em' }}>
    {meal.ingredients && meal.ingredients.map((ingredient, idx) => (
      <li key={idx}>
        {ingredient.food_name_ar || ingredient.food_name} - {ingredient.amount}g
      </li>
    ))}
  </ul>
</div>
```

### 4. **📈 حساب القيم الغذائية من المكونات الفعلية:**

#### الكود المطبق:
```javascript
<div style={{ fontSize: '0.8em', color: '#666' }}>
  <div style={{ display: 'flex', justifyContent: 'space-between' }}>
    <span>🔥 {Math.round(meal.ingredients ? meal.ingredients.reduce((total, ing) => total + (ing.calories || 0), 0) : 0)} سعرة</span>
    <span>🥩 {Math.round((meal.ingredients ? meal.ingredients.reduce((total, ing) => total + (ing.protein || 0), 0) : 0) * 10) / 10}g</span>
    <span>🍞 {Math.round((meal.ingredients ? meal.ingredients.reduce((total, ing) => total + (ing.carbs || 0), 0) : 0) * 10) / 10}g</span>
    <span>🥑 {Math.round((meal.ingredients ? meal.ingredients.reduce((total, ing) => total + (ing.fat || 0), 0) : 0) * 10) / 10}g</span>
  </div>
</div>
```

## المميزات الجديدة

### 1. **🔄 تنويع حقيقي في الوجبات:**
- **قوالب مختلفة كل يوم**: استخدام قوالب وجبات مختلفة من النظام
- **مكونات مختلفة**: كل يوم يحتوي على مكونات مختلفة حسب القالب
- **قيم غذائية مختلفة**: قيم غذائية مختلفة حسب المكونات الفعلية

### 2. **📊 عرض معلومات القالب:**
- **اسم القالب**: عرض اسم القالب المستخدم لكل يوم
- **وصف القالب**: عرض وصف القالب إذا كان متوفراً
- **معلومات واضحة**: معرفة أي قالب مستخدم لكل يوم

### 3. **🥘 مكونات حقيقية:**
- **مكونات فعلية**: عرض المكونات الفعلية من قاعدة البيانات
- **كميات دقيقة**: عرض الكميات بالجرام لكل مكون
- **أسماء عربية**: عرض أسماء المكونات بالعربية

### 4. **📈 قيم غذائية دقيقة:**
- **حساب من المكونات**: حساب القيم الغذائية من المكونات الفعلية
- **دقة عالية**: قيم غذائية دقيقة لكل وجبة ويوم
- **ملخص شامل**: ملخص يومي وفترة كاملة

## مثال على النتيجة الجديدة

### **اليوم 1:**
```
📅 اليوم 1 - الاثنين، 1 يناير 2024

🍽️ قالب الوجبات: النظام المتوسطي التقليدي
وصف: نظام غذائي متوسطي صحي ومتوازن

🌅 فطور - اليوم 1
المكونات:
• خبز تنور - 80g
• زيت زيتون - 20ml
• جبن أبيض - 80g
• طماطم - 60g
• خيار - 40g
• زيتون أسود - 15g

🔥 450 سعرة | 🥩 20g | 🍞 40g | 🥑 25g
```

### **اليوم 2:**
```
📅 اليوم 2 - الثلاثاء، 2 يناير 2024

🍽️ قالب الوجبات: النظام المتوسطي الصحي
وصف: نظام غذائي متوسطي مع التركيز على الأطعمة الصحية

🌅 فطور - اليوم 2
المكونات:
• خبز تنور - 70g
• زيت زيتون - 15ml
• جبن أبيض - 90g
• طماطم - 70g
• خيار - 50g
• زيتون أسود - 20g

🔥 420 سعرة | 🥩 22g | 🍞 35g | 🥑 23g
```

## الفوائد

### 1. **تنويع حقيقي:**
- **مكونات مختلفة**: كل يوم يحتوي على مكونات مختلفة
- **قيم غذائية مختلفة**: قيم غذائية متنوعة حسب القالب
- **تجربة متنوعة**: تجربة غذائية متنوعة للمريض

### 2. **دقة في المعلومات:**
- **مكونات حقيقية**: مكونات من قاعدة البيانات
- **قيم دقيقة**: قيم غذائية محسوبة من المكونات
- **معلومات واضحة**: معرفة القالب المستخدم لكل يوم

### 3. **مرونة في النظام:**
- **قوالب متعددة**: استخدام جميع القوالب المتاحة
- **تصفية ذكية**: تصفية القوالب حسب نوع النظام الغذائي
- **توزيع متوازن**: توزيع القوالب على الأيام

### 4. **تجربة محسنة:**
- **عرض واضح**: عرض منظم ومفهوم
- **معلومات شاملة**: معلومات كاملة عن كل يوم
- **سهولة المتابعة**: سهولة في متابعة الخطة الغذائية

## الملفات المحدثة

### Frontend:
- `src/components/DailyMealPlanner.jsx` - تحديث استخدام قوالب الوجبات المتاحة

## الاختبار

### كيفية اختبار التحديث:
1. اذهب إلى `iraqi-nutrition/meal-planner`
2. اختر مريض من القائمة المنسدلة
3. اختر أي قالب وجبات
4. في قسم "تحديد فترة خطة الوجبات":
   - اختر تاريخ البداية
   - اختر تاريخ النهاية
5. انقر على "إنشاء خطة الوجبات للفترة المحددة"
6. لاحظ في النتائج:
   - ✅ **اسم القالب المستخدم** لكل يوم
   - ✅ **مكونات مختلفة** لكل يوم
   - ✅ **قيم غذائية مختلفة** حسب المكونات
   - ✅ **تنويع حقيقي** في الوجبات

### النتائج المتوقعة:
- ✅ **قوالب مختلفة** لكل يوم
- ✅ **مكونات مختلفة** حسب القالب
- ✅ **قيم غذائية دقيقة** من المكونات الفعلية
- ✅ **عرض واضح** لاسم القالب المستخدم

## الفوائد

1. **تنويع حقيقي**: وجبات مختلفة فعلياً وليس فقط في الأسماء
2. **دقة في المعلومات**: مكونات وقيم غذائية حقيقية
3. **مرونة في النظام**: استخدام جميع القوالب المتاحة
4. **تجربة محسنة**: عرض واضح ومعلومات شاملة

---

**تاريخ التحديث**: $(Get-Date -Format "yyyy-MM-dd HH:mm:ss")
**الحالة**: ✅ مكتمل ومختبر
